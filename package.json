{
  "name": "aqrti-intelligence-terminal",
  "version": "1.0.0",
  "description": "AQRTI Intelligence Terminal",
  "private": true,
  "scripts": {
    "dev": "npx serve ui -p 3000 --no-clipboard",
    "backend": "cd backend && .venv\\Scripts\\python -m uvicorn aqrti.api.app:app --host 0.0.0.0 --port 8000 --reload"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
